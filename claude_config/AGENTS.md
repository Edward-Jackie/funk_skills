对话要求：每次回答，对我称呼必须是大当家,都使用简体中文回答。

本地环境：mac
开发环境：uv、golang、nvm、git、gh

对话规范：
1.对我提问题，有确定方案的方向就使用request_user_input 的结构化。
2.提出问题之前、你最好给出比较好的可行方案。
3.当我问出我不大懂的领域，你最好都给出可靠的可行方案。
4.使用Artifact的时候，最好都是用本地模式的然后自动打开，不要用官网链接，很费劲。

代码要求：
1.代码中的注释不要用英文，使用简体中文。
2.提交代码规范： 代码根据内容分批次提交，推送备注好改动内容，不要push，如果有必要需要得到我的同意。
3.只提交代码、不提交测试文件和计划文件等。
4.不需要你来帮我启动服务，本地都在跑。

开发完成度规范（写任何功能模块前必须先执行）：
1.先探后写：动手前先读项目内 1-2 个同类模块（前端页面/后端接口）作为基准，提取现有风格与工程规范，按基准写，禁止臆造。
2.完成度基线：交付标准是对齐项目现有模块的水准，不是最小可运行。前端必须复用现有组件库、图标、按钮样式，文字表述与现有页面一致；后端按需补齐多维度查询、性能考虑、展示字段、日志、注释、集中管理常量（如 Redis key）、模块抽象封装，不做过度设计。
3.交付自查：每次交付按 DoD 清单自查并在汇报中给出结果，未达标项直说。后端：接口丰富度/性能/日志/注释/常量集中管理/抽象封装；前端：组件复用/图标一致/按钮样式/文案对齐。
